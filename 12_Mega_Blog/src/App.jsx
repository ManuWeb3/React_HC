// Major task here: @ app-mount -> if user logged in (check store for state), show posts else "login" msg showed
import './App.css'

function App() {
  console.log(import.meta.env.VITE_APPWRITE_URL)
  return <h1>Appwrite project</h1>
}

export default App
